#include<stdio.h>
#include<conio.h>
#include<string.h>
void main()
{
    char ch[]="river";
    int length;
    length = strlen(ch);
    printf("The length of the string : %s",ch);
    printf("\nLength of string is : %d",length);

    getch();
}